module.exports = ({ id, email, firstName, grossPay, basicSalary, deductions, netPay, overtimePay }) => {
    const today = new Date();

return `
    <!doctype html>
    <html>
        <head>
            <meta charset="utf-8">
`
}